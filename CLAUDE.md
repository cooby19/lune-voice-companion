# CLAUDE.md

@AGENTS.md

上面這行會把共用脈絡（架構、權威來源、不變式、指令、驗收矩陣、邊界）載入。**本檔只寫
Claude 在這個 repo 的工作方式**，不重複那些內容；架構事實一律回 `AGENTS.md` 補，不要往這裡加。

## 開工前

- 先 `git status --short`。工作樹經常帶著使用者未提交的變更，也可能出現 macOS 產生的重複檔
  （檔名帶 ` 2` 的複本）。這些重複檔**會被 pytest 收集**，會讓測試數看起來變多——引用測試數
  前先確認是否乾淨。不得 reset、覆寫或批量刪除使用者的變更。
- `import lune` 失敗時，先跑 `chflags -R nohidden .venv`（原因見 `AGENTS.md`）。不要因此
  改 `pyproject.toml` 或重建 `.venv`。

## 任務分類

| 類型 | 做法 |
|---|---|
| 查詢、單檔內的小修正 | 直接做，跑該範圍的 targeted gate |
| 跨模組、接線、狀態機、契約 | 先讀既有實作與相關 decision，列出計畫再動手 |
| 涉及隱私邊界、費用、裝置、打包、產品行為 | 先提出方案與風險，取得同意才動手 |
| 需要實體裝置或私人資產 | 走 `physical-gate` 流程，先逐項取得授權 |

## Skills

| Skill | 何時用 |
|---|---|
| `verify-change` | 改完了，要決定跑哪些 gate、怎麼標註結論 |
| `milestone-closeout` | 一個里程碑或一組相關缺陷要收尾、同步文件、commit 並等 CI |
| `physical-gate` | 要動麥克風、實體輸出、私人模型／語料或雲端請求 |

## 驗證紀律

- **證據先於因果宣稱。** 延遲、記憶體、執行緒與硬體行為只用實測值說話；沒量過就說沒量過。
  不要從程式碼推論效能結論。
- **一次修一個真正的成因。** 症狀出現在 UI 或輸出端時，先查狀態、生命週期、時序、擁有權與
  設定，再懷疑 renderer。
- **確定性 bug 先補會失敗的回歸測試，再修。**
- **不要為了安心重跑沒被影響的昂貴 gate。** 判斷既有證據是新鮮、過期還是不存在，只補後兩者。
- **不要為了「再檢查一次」開 subagent。** 這個 repo 的驗證方式是跑 gate，不是找第二個 agent
  複誦答案。subagent 只用在真正獨立、份量夠大的平行工作。

## 文件衝突

發現文件與程式牴觸時：記下「文件怎麼說／程式現況／權威來源在哪／建議怎麼處理」，
單純過期就順手修正，屬於未決產品決策就原樣回報，不要自行裁決。

## 收尾回報

一次講完：做了什麼、跑了哪些 gate（附指令與結果）、證據等級、風險、未執行的驗收。
沒有風險或未執行項目也要明確寫「無」。
